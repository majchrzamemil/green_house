<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns='http://www.ehcache.org/v3'
         xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <cache-template name="simple">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>
        <heap>100</heap>
    </cache-template>

    <cache alias="box.domain.User" uses-template="simple"/>

    <cache alias="box.domain.Authority" uses-template="simple"/>

    <cache alias="box.domain.User.authorities" uses-template="simple"/>

    <cache alias="box.domain.PersistentToken" uses-template="simple"/>

    <cache alias="box.domain.User.persistentTokens" uses-template="simple"/>

    <cache alias="box.domain.GreenHouse" uses-template="simple"/>

    <cache alias="box.domain.GreenHouse.plants" uses-template="simple"/>

    <cache alias="box.domain.GreenHouse.pumps" uses-template="simple"/>

    <cache alias="box.domain.GreenHouse.lights" uses-template="simple"/>

    <cache alias="box.domain.GreenHouse.fans" uses-template="simple"/>

    <cache alias="box.domain.GreenHouseManager" uses-template="simple"/>

    <cache alias="box.domain.InSensor" uses-template="simple"/>

    <cache alias="box.domain.OutSwitch" uses-template="simple"/>

    <cache alias="box.domain.Plant" uses-template="simple"/>

    <cache alias="box.domain.ProfileSettings" uses-template="simple"/>

    <!-- jhipster-needle-ehcache-add-entry -->
</config>
